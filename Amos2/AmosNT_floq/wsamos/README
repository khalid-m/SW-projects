Package wsamos - a generic web service for calling Amos functions.

Test client accessing the Amos web service installed at 130.238.11.125 (Johan's machine).
Execute:

1. cd %AMOS_HOME%\wsamos
2. setupclient
3. compile
4. run