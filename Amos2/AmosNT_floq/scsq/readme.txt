SCSQ is compiled and installed with the script
   install.cmd

The install script calls the installer for Amos II in
bin/install.bat. Make sure first that you can install Amos II.

To make a small test of SQSQ run the script
   test.cmd

For a complete test run
   testall.cmd


