first of all Install the SCSQ-trie located in ..\SCSQ-trie

To test different indexing and deletion sterategies:

Shortcut: Run Test_all_indexes.cmd to test all 6 indexing structures in LRB.

OR:

1-go to src folder
2-replace visited.lsp with  appropriate visited.lsp file from src\visited.lsp_versions
3-Install LRB by running install.cmd
4-run LRB by executing lr.cmd (Read more about how to ru the LRB in LRB_Usage_guide.txt)